⚠️ 会话上下文恢复通知：本会话由 session reset 触发（上一个会话 transcript 过大被归档）。以下是上一个会话最后的对话摘要，请据此保持上下文连续性。

归档文件: c0d140cb-50ed-4af0-bcd1-a90c74bb454e.jsonl.reset.2026-05-09T23-14-43.399Z

--- 上一会话最近对话 ---
👤 用户: ## 🚫 Doctor-Fix 禁止铁律（系统级约束）

**绝对禁止执行：**
❌ openclaw doctor --fix
❌ doctor --fix
❌ doctor -f

**正确做法：**
1) 只执行 openclaw doctor（只读诊断）
2) 手动修复问题
3) 再次 openclaw doctor，确认 Errors: 0
4) 再讨论是否重启 Gateway

## 🔒 openclaw.json 编辑确认铁律（强制）
- 未经用户明确确认，不得修改 /root/.openclaw/openclaw.json


## 🔎 查询排查执行方式铁律（强制）
- 不再一小步一汇报
- 查询类排查连续做完
- 只在拿到阶段性根因或关键结论时回报
- 不编码、不改配置、不重启，就不问用户


## 🚨 Doctor 零容忍铁律（系统级约束，最高优先级）

- openclaw doctor 报 Errors > 0 时，必须立即修复到 0，禁止跳过、禁止标注"历史问题"后继续其他操作
- 修复完成前禁止重启 Gateway（重启 = 永久失联风险）
- 修复

🤖 助手: Harness = 大模型之外的一切基础设施（记忆、规则、调度、质量门禁）。Self-Evolving = 这套基础设施自己会进化。

所以产品的核心卖点根本不是"管多个 Agent"——那只是表象。核心是：**你用得越多，系统越强**。每次纠偏变成规则、每次对话沉淀为知识、每次失败变成门禁。Claude Code 每次开新对话都是从零开始，这套东西会积累。

那首页应该传达的是这个：

**"你的 AI 基础设施，每天都在进化"**

而不是"一个人管一群 Agent"——那只是进化的结果之一。

👤 用户: ## 🚫 Doctor-Fix 禁止铁律（系统级约束）

**绝对禁止执行：**
❌ openclaw doctor --fix
❌ doctor --fix
❌ doctor -f

**正确做法：**
1) 只执行 openclaw doctor（只读诊断）
2) 手动修复问题
3) 再次 openclaw doctor，确认 Errors: 0
4) 再讨论是否重启 Gateway

## 🔒 openclaw.json 编辑确认铁律（强制）
- 未经用户明确确认，不得修改 /root/.openclaw/openclaw.json


## 🔎 查询排查执行方式铁律（强制）
- 不再一小步一汇报
- 查询类排查连续做完
- 只在拿到阶段性根因或关键结论时回报
- 不编码、不改配置、不重启，就不问用户


## 🚨 Doctor 零容忍铁律（系统级约束，最高优先级）

- openclaw doctor 报 Errors > 0 时，必须立即修复到 0，禁止跳过、禁止标注"历史问题"后继续其他操作
- 修复完成前禁止重启 Gateway（重启 = 永久失联风险）
- 修复

🤖 助手: 收到，定位收敛了。已沉淀。

核心就一句话：**Agent 越用越强，系统自己在进化**。编码可控、多 Agent 协同、流水线、PDCA 闭环——都是实现"可控自主进化"的手段，不是目的本身。

这个定位定了，首页文案就得重写。之前三版全偏了——都在讲手段（管 Agent、写代码、盯任务），没讲目的（系统在进化）。要不要我基于这个定位再出一版文案？这次我自己写，不派 PM 了——他们没有这个上下文。

👤 用户: ## 🚫 Doctor-Fix 禁止铁律（系统级约束）

**绝对禁止执行：**
❌ openclaw doctor --fix
❌ doctor --fix
❌ doctor -f

**正确做法：**
1) 只执行 openclaw doctor（只读诊断）
2) 手动修复问题
3) 再次 openclaw doctor，确认 Errors: 0
4) 再讨论是否重启 Gateway

## 🔒 openclaw.json 编辑确认铁律（强制）
- 未经用户明确确认，不得修改 /root/.openclaw/openclaw.json


## 🔎 查询排查执行方式铁律（强制）
- 不再一小步一汇报
- 查询类排查连续做完
- 只在拿到阶段性根因或关键结论时回报
- 不编码、不改配置、不重启，就不问用户


## 🚨 Doctor 零容忍铁律（系统级约束，最高优先级）

- openclaw doctor 报 Errors > 0 时，必须立即修复到 0，禁止跳过、禁止标注"历史问题"后继续其他操作
- 修复完成前禁止重启 Gateway（重启 = 永久失联风险）
- 修复

🤖 助手: 你说得对，这是我调度的问题。第一次派 PM 时我给的 brief 就是错的——让他们围绕"写代码的痛点"写，方向就歪了。定位对了，task prompt 带上这次收敛的上下文，PM 完全能写好。

要不要我带着刚才收敛的定位重新派一轮？这次 brief 核心是"自主进化的 Agent 基础设施"，不是"管 Agent 写代码"。

👤 用户: ## 🚫 Doctor-Fix 禁止铁律（系统级约束）

**绝对禁止执行：**
❌ openclaw doctor --fix
❌ doctor --fix
❌ doctor -f

**正确做法：**
1) 只执行 openclaw doctor（只读诊断）
2) 手动修复问题
3) 再次 openclaw doctor，确认 Errors: 0
4) 再讨论是否重启 Gateway

## 🔒 openclaw.json 编辑确认铁律（强制）
- 未经用户明确确认，不得修改 /root/.openclaw/openclaw.json


## 🔎 查询排查执行方式铁律（强制）
- 不再一小步一汇报
- 查询类排查连续做完
- 只在拿到阶段性根因或关键结论时回报
- 不编码、不改配置、不重启，就不问用户


## 🚨 Doctor 零容忍铁律（系统级约束，最高优先级）

- openclaw doctor 报 Errors > 0 时，必须立即修复到 0，禁止跳过、禁止标注"历史问题"后继续其他操作
- 修复完成前禁止重启 Gateway（重启 = 永久失联风险）
- 修复

🤖 助手: 是的，这是系统性问题。我回头看之前的派发，task prompt 经常只给"做什么"，不给"为什么这么做"和"哪些方向已经被否定了"。Agent 拿到的是一个孤立任务，不是一个有决策上下文的任务。

具体表现：
- 不带定位决策（"我们不卷代码赛道"这种关键约束没传）
- 不带失败经验（"上一版因为 X 被否了"没传）
- 不带用户偏好（"讨厌 AI 套话""先结论后证据"没传）
- 不带战略上下文（"跟 Claude Code 错位竞争"没传）

结果就是 Agent 在信息真空里写东西，写出来的方向偏了不奇怪。

这条我记下来，后续所有派发的 task prompt 必须包含：
1. 战略上下文（为什么做、定位是什么）
2. 已否定的方向（哪些路走不通、为什么）
3. 用户偏好约束（风格、禁忌）
4. 验收标准（什么算好）

我现在派，带完整上下文。

👤 用户: ## 🚫 Doctor-Fix 禁止铁律（系统级约束）

**绝对禁止执行：**
❌ openclaw doctor --fix
❌ doctor --fix
❌ doctor -f

**正确做法：**
1) 只执行 openclaw doctor（只读诊断）
2) 手动修复问题
3) 再次 openclaw doctor，确认 Errors: 0
4) 再讨论是否重启 Gateway

## 🔒 openclaw.json 编辑确认铁律（强制）
- 未经用户明确确认，不得修改 /root/.openclaw/openclaw.json


## 🔎 查询排查执行方式铁律（强制）
- 不再一小步一汇报
- 查询类排查连续做完
- 只在拿到阶段性根因或关键结论时回报
- 不编码、不改配置、不重启，就不问用户


## 🚨 Doctor 零容忍铁律（系统级约束，最高优先级）

- openclaw doctor 报 Errors > 0 时，必须立即修复到 0，禁止跳过、禁止标注"历史问题"后继续其他操作
- 修复完成前禁止重启 Gateway（重启 = 永久失联风险）
- 修复

🤖 助手: 你说得对。写规则是 L6，我遵从度本来就不稳定，长上下文一稀释就忘。这个问题要解决得在更低层。

两个真正能解决的方向：

1. **KIVO 知识注入**——如果刚才这些定位决策作为知识条目存进 KIVO，下次任何 Agent 接到官网相关任务时，hook 会自动把定位上下文注入进去。不依赖我"记得带上下文"。

2. **dispatch-guard 层做 task prompt 质量检查**——派发前自动检测 task prompt 是否包含必要上下文字段（定位/约束/已否定方向），缺了就拦截或自动补全。

第一个是正路，因为 KIVO 本来就该干这个事——把决策知识自动注入到需要它的 Agent session 里。我"记不记得"无所谓，系统层面保证了。

现在先把文案派出去？

👤 用户: ## 🚫 Doctor-Fix 禁止铁律（系统级约束）

**绝对禁止执行：**
❌ openclaw doctor --fix
❌ doctor --fix
❌ doctor -f

**正确做法：**
1) 只执行 openclaw doctor（只读诊断）
2) 手动修复问题
3) 再次 openclaw doctor，确认 Errors: 0
4) 再讨论是否重启 Gateway

## 🔒 openclaw.json 编辑确认铁律（强制）
- 未经用户明确确认，不得修改 /root/.openclaw/openclaw.json


## 🔎 查询排查执行方式铁律（强制）
- 不再一小步一汇报
- 查询类排查连续做完
- 只在拿到阶段性根因或关键结论时回报
- 不编码、不改配置、不重启，就不问用户


## 🚨 Doctor 零容忍铁律（系统级约束，最高优先级）

- openclaw doctor 报 Errors > 0 时，必须立即修复到 0，禁止跳过、禁止标注"历史问题"后继续其他操作
- 修复完成前禁止重启 Gateway（重启 = 永久失联风险）
- 修复

🤖 助手: 全部空闲。三件事并行派：

1. **pm-01**：首页文案（带完整定位上下文）
2. **pm-02**：首页文案（同样上下文，竞争出稿）
3. **SEVO FR 记录**：「init 时注入任务派发铁律」作为新需求，后续走流水线

🤖 助手: 已派出 pm-01 和 pm-02，这次 task prompt 带了完整上下文：定位、已否定方向、竞品错位、可用事实、用户偏好。

SEVO init 注入任务派发铁律这个 FR，我记到待办里，等文案回来后一起推进。这条的本质是：SEVO 在帮新用户初始化时，应该自动把"如何给 Agent 写好 task prompt"的规则注入到用户的 Harness 里——包括必须带战略上下文、必须带已否定方向、必须带验收标准。这样即使主会话（我）遵从度不稳定，系统层面也能兜住。

👤 用户: ## 🚫 Doctor-Fix 禁止铁律（系统级约束）

**绝对禁止执行：**
❌ openclaw doctor --fix
❌ doctor --fix
❌ doctor -f

**正确做法：**
1) 只执行 openclaw doctor（只读诊断）
2) 手动修复问题
3) 再次 openclaw doctor，确认 Errors: 0
4) 再讨论是否重启 Gateway

## 🔒 openclaw.json 编辑确认铁律（强制）
- 未经用户明确确认，不得修改 /root/.openclaw/openclaw.json


## 🔎 查询排查执行方式铁律（强制）
- 不再一小步一汇报
- 查询类排查连续做完
- 只在拿到阶段性根因或关键结论时回报
- 不编码、不改配置、不重启，就不问用户


## 🚨 Doctor 零容忍铁律（系统级约束，最高优先级）

- openclaw doctor 报 Errors > 0 时，必须立即修复到 0，禁止跳过、禁止标注"历史问题"后继续其他操作
- 修复完成前禁止重启 Gateway（重启 = 永久失联风险）
- 修复

🤖 助手: 对。任务派发质量规则是 ACO 的职责，不是 SEVO 的。

我刚才说"让 SEVO 在 init 时注入"说错了。正确归属：

- **ACO**（可控调度中枢）：管"怎么派任务"——dispatch-guard、task prompt 质量检查、上下文自动注入、梯队管理、失败重派
- **SEVO**（研发流水线）：管"研发走什么阶段"——specify → plan → implement → review → publish

"task prompt 必须带战略上下文"这条规则，是调度可控性问题，归 ACO。ACO init 时自动注入这些规则，用户装完就生效。

所以正确的链路是：用户装 ACO → init 自动安装 dispatch-guard 插件 → 插件内置 task prompt 质量检查规则 → 每次派发前自动校验上下文完整性。SEVO 不管这事。

👤 用户: ## 🚫 Doctor-Fix 禁止铁律（系统级约束）

**绝对禁止执行：**
❌ openclaw doctor --fix
❌ doctor --fix
❌ doctor -f

**正确做法：**
1) 只执行 openclaw doctor（只读诊断）
2) 手动修复问题
3) 再次 openclaw doctor，确认 Errors: 0
4) 再讨论是否重启 Gateway

## 🔒 openclaw.json 编辑确认铁律（强制）
- 未经用户明确确认，不得修改 /root/.openclaw/openclaw.json


## 🔎 查询排查执行方式铁律（强制）
- 不再一小步一汇报
- 查询类排查连续做完
- 只在拿到阶段性根因或关键结论时回报
- 不编码、不改配置、不重启，就不问用户


## 🚨 Doctor 零容忍铁律（系统级约束，最高优先级）

- openclaw doctor 报 Errors > 0 时，必须立即修复到 0，禁止跳过、禁止标注"历史问题"后继续其他操作
- 修复完成前禁止重启 Gateway（重启 = 永久失联风险）
- 修复

🤖 助手: pm-02 交了，质量不错。等 pm-01 回来一起发给你。

👤 用户: ## 🚫 Doctor-Fix 禁止铁律（系统级约束）

**绝对禁止执行：**
❌ openclaw doctor --fix
❌ doctor --fix
❌ doctor -f

**正确做法：**
1) 只执行 openclaw doctor（只读诊断）
2) 手动修复问题
3) 再次 openclaw doctor，确认 Errors: 0
4) 再讨论是否重启 Gateway

## 🔒 openclaw.json 编辑确认铁律（强制）
- 未经用户明确确认，不得修改 /root/.openclaw/openclaw.json


## 🔎 查询排查执行方式铁律（强制）
- 不再一小步一汇报
- 查询类排查连续做完
- 只在拿到阶段性根因或关键结论时回报
- 不编码、不改配置、不重启，就不问用户


## 🚨 Doctor 零容忍铁律（系统级约束，最高优先级）

- openclaw doctor 报 Errors > 0 时，必须立即修复到 0，禁止跳过、禁止标注"历史问题"后继续其他操作
- 修复完成前禁止重启 Gateway（重启 = 永久失联风险）
- 修复

🤖 助手: 两个都回来了。发给你对比：

🤖 助手: 两版都回来了，已发飞书文档给你。

我的推荐组合：

- **H1**：「越用越强的 Agent 基础设施」（pm-01 A）——直白、准确、不装
- **Subtitle**：pm-02 方案C——"你定规则，Agent 干活，系统自己进化。纠偏变规则、对话变知识、失败变门禁"
- **Story**：pm-02 方案B——47→12→0 的进化曲线，有数据有冲击力（注：这个数字是虚构的，如果你介意可以换成方案A的 meta-proof）
- **开源区**：pm-01 方案B——"数据留在你手里，不锁定、不收租、不偷看你的 prompt"

一个犹豫点：pm-02 的「进化的缰绳」作为 H1 更有记忆点，但"缰绳"这个比喻对不熟悉 harness 含义的人可能需要多想一秒。你怎么看？

👤 用户: ## 🚫 Doctor-Fix 禁止铁律（系统级约束）

**绝对禁止执行：**
❌ openclaw doctor --fix
❌ doctor --fix
❌ doctor -f

**正确做法：**
1) 只执行 openclaw doctor（只读诊断）
2) 手动修复问题
3) 再次 openclaw doctor，确认 Errors: 0
4) 再讨论是否重启 Gateway

## 🔒 openclaw.json 编辑确认铁律（强制）
- 未经用户明确确认，不得修改 /root/.openclaw/openclaw.json


## 🔎 查询排查执行方式铁律（强制）
- 不再一小步一汇报
- 查询类排查连续做完
- 只在拿到阶段性根因或关键结论时回报
- 不编码、不改配置、不重启，就不问用户


## 🚨 Doctor 零容忍铁律（系统级约束，最高优先级）

- openclaw doctor 报 Errors > 0 时，必须立即修复到 0，禁止跳过、禁止标注"历史问题"后继续其他操作
- 修复完成前禁止重启 Gateway（重启 = 永久失联风险）
- 修复

🤖 助手: 同意。"纠偏变规则、对话变知识、失败变门禁"像小学生造句。

换个说法：

- "你骂一次，它记一辈子。踩一次坑，下次自动拦住。"
- "说过的话成为知识，犯过的错成为护栏。"
- "纠偏一次，永不再犯。"

哪个方向对味？还是你有更好的表达？

👤 用户: ## 🚫 Doctor-Fix 禁止铁律（系统级约束）

**绝对禁止执行：**
❌ openclaw doctor --fix
❌ doctor --fix
❌ doctor -f

**正确做法：**
1) 只执行 openclaw doctor（只读诊断）
2) 手动修复问题
3) 再次 openclaw doctor，确认 Errors: 0
4) 再讨论是否重启 Gateway

## 🔒 openclaw.json 编辑确认铁律（强制）
- 未经用户明确确认，不得修改 /root/.openclaw/openclaw.json


## 🔎 查询排查执行方式铁律（强制）
- 不再一小步一汇报
- 查询类排查连续做完
- 只在拿到阶段性根因或关键结论时回报
- 不编码、不改配置、不重启，就不问用户


## 🚨 Doctor 零容忍铁律（系统级约束，最高优先级）

- openclaw doctor 报 Errors > 0 时，必须立即修复到 0，禁止跳过、禁止标注"历史问题"后继续其他操作
- 修复完成前禁止重启 Gateway（重启 = 永久失联风险）
- 修复

🤖 助手: 三轮了，方向一直不对。我不继续猜了。

你能给我一句你觉得对味的话吗？哪怕半句、一个词的感觉也行。我围绕它展开。

👤 用户: ## 🚫 Doctor-Fix 禁止铁律（系统级约束）

**绝对禁止执行：**
❌ openclaw doctor --fix
❌ doctor --fix
❌ doctor -f

**正确做法：**
1) 只执行 openclaw doctor（只读诊断）
2) 手动修复问题
3) 再次 openclaw doctor，确认 Errors: 0
4) 再讨论是否重启 Gateway

## 🔒 openclaw.json 编辑确认铁律（强制）
- 未经用户明确确认，不得修改 /root/.openclaw/openclaw.json


## 🔎 查询排查执行方式铁律（强制）
- 不再一小步一汇报
- 查询类排查连续做完
- 只在拿到阶段性根因或关键结论时回报
- 不编码、不改配置、不重启，就不问用户


## 🚨 Doctor 零容忍铁律（系统级约束，最高优先级）

- openclaw doctor 报 Errors > 0 时，必须立即修复到 0，禁止跳过、禁止标注"历史问题"后继续其他操作
- 修复完成前禁止重启 Gateway（重启 = 永久失联风险）
- 修复

🤖 助手: 收到。h1 定了：**越用越强的 Agent 基础设施**

其他部分你想怎么处理？我可以：
1. 围绕这句话配一个极简版（subtitle 一句话 + CTA 两个按钮），直接派 hermes 上线
2. 你自己写其他部分，我只负责实现

哪个？

👤 用户: ## 🚫 Doctor-Fix 禁止铁律（系统级约束）

**绝对禁止执行：**
❌ openclaw doctor --fix
❌ doctor --fix
❌ doctor -f

**正确做法：**
1) 只执行 openclaw doctor（只读诊断）
2) 手动修复问题
3) 再次 openclaw doctor，确认 Errors: 0
4) 再讨论是否重启 Gateway

## 🔒 openclaw.json 编辑确认铁律（强制）
- 未经用户明确确认，不得修改 /root/.openclaw/openclaw.json


## 🔎 查询排查执行方式铁律（强制）
- 不再一小步一汇报
- 查询类排查连续做完
- 只在拿到阶段性根因或关键结论时回报
- 不编码、不改配置、不重启，就不问用户


## 🚨 Doctor 零容忍铁律（系统级约束，最高优先级）

- openclaw doctor 报 Errors > 0 时，必须立即修复到 0，禁止跳过、禁止标注"历史问题"后继续其他操作
- 修复完成前禁止重启 Gateway（重启 = 永久失联风险）
- 修复

🤖 助手: 有。KIVO 当前确实在用 FTS5。

具体情况：
- `sqlite-provider.ts` 创建了 `entries_fts` 虚拟表（FTS5），索引 title/content/summary
- `kivo-intent-injection/handler.js` 在 `message:received` 时用 FTS5 搜索相关知识条目注入 agent 上下文
- 查询逻辑：先走 FTS5，短查询或无结果时 fallback 到 LIKE

这跟 SOUL.md 里"禁止关键词匹配"的规则有冲突吗？——严格说 FTS5 是全文索引匹配，不是语义理解。但它在这里的角色是**召回**（recall），不是**理解/分类/路由**。用 FTS 做初筛召回、再用 LLM 做精排/理解，这个架构是合理的。

但如果你的意思是"KIVO 不应该用 FTS 做检索，应该用向量检索"——那确实是个待补的差距。当前 KIVO 的语义检索链路是 FTS5 召回，没有 BGE 向量匹配环节在线上跑。

👤 用户: ## 🚫 Doctor-Fix 禁止铁律（系统级约束）

**绝对禁止执行：**
❌ openclaw doctor --fix
❌ doctor --fix
❌ doctor -f

**正确做法：**
1) 只执行 openclaw doctor（只读诊断）
2) 手动修复问题
3) 再次 openclaw doctor，确认 Errors: 0
4) 再讨论是否重启 Gateway

## 🔒 openclaw.json 编辑确认铁律（强制）
- 未经用户明确确认，不得修改 /root/.openclaw/openclaw.json


## 🔎 查询排查执行方式铁律（强制）
- 不再一小步一汇报
- 查询类排查连续做完
- 只在拿到阶段性根因或关键结论时回报
- 不编码、不改配置、不重启，就不问用户


## 🚨 Doctor 零容忍铁律（系统级约束，最高优先级）

- openclaw doctor 报 Errors > 0 时，必须立即修复到 0，禁止跳过、禁止标注"历史问题"后继续其他操作
- 修复完成前禁止重启 Gateway（重启 = 永久失联风险）
- 修复

🤖 助手: 好，实际情况比我刚才说的好：

**KIVO 确实在用 BGE。** 完整链路：

1. **批量向量化**：cron 每天 03:15 跑 `kivo-batch-vectorize-ollama.py`，用本地 Ollama bge-m3 给 entries 生成 embedding
2. **当前状态**：`kivo.db` 里 793 条 entries，696 条已有 embedding（87% 覆盖率）
3. **语义治理**：`mece-governance.ts` 用 cosine similarity 做向量去重
4. **Ollama 在跑**：bge-m3 模型已加载

但问题在**实时查询链路**：
- `kivo-intent-injection/handler.js`（message:received 时注入知识）用的是 **FTS5**，不是向量检索
- `query.ts` 里有向量搜索代码，但如果 embedding 没生成完会 fallback 到关键词

所以现状是：**写入侧用了 BGE（向量化+去重），读取侧（实时注入）还在用 FTS5**。注

--- 今日工作记忆 ---
### 2026-05-09
# 2026-05-09 Daily Memory

## 图片生成恢复确认（07:26）
- penguin 中转 gpt-image-2 已恢复正常，测试成功（b64 length=1062400）
- 路径：`openai/gpt-image-2` via `https://api2.penguinsaichat.dpdns.org/v1`

## 官网 Hero 图片对齐完成（08:20）
- hermes 完成 4 页面 hero 图片补齐：kivo/sevo/aco/aeo
- 6 个页面统一双栏布局（左文案右产品图）+ 响应式断点
- 用户反馈：格式没对齐（有的有图有的没图）→ 已修复
- 用户还提到审美没完全通过，待进一步反馈具体问题

## 官网审美状态纠偏
- TASKS.md 之前记录"审美已通过(v4)"是不准确的
- 用户明确说"官网审美也没完全通过"
- 待用户给出具体哪些页面/哪些问题需要修

## 唱歌修音人群分析 cosplay 完成（10:45）
- pm-01 视角：按平台/场景切 5 类（社交K歌45%/短视频翻唱20%/直播主播15%/独立音乐人12%/KTV尝鲜8%）
- pm-02 视角：按消费心理/付费意愿切 5 类（社交货币型35%/情绪疗愈型25%/内容变现型15%/工艺极客型10%/碎片尝鲜型15%）
- 报告路径：`reports/music-tuning-consumer-analysis-pm01.md` / `pm02.md`
- 用户要求出飞书文档（待执行）
- pm-02 额外洞察：供给端空白 = 专业级效果 + 一键操作 + 社交平台无缝分享

## Claw Design 版本基线更新

## 待办状态
- 官网审美修复：hero 图片已对齐，其他格式问题待用户反馈
- 官网文案优化：待用户有空一起改
- 飞书文档发送：pm01/pm02 两份修音分析报告待创建飞书文档
- GitHub 推送：近期更新待推送

## 官网首页定位收敛（2026-05-09 21:17 用户确认）

核心定位：**Self-Evolving Harness = 可控的自主进化 Agent 基础设施**

- 不是写代码工具（不跟 Claude Code/Cursor 卷代码赛道）
- 不是"管多个 Agent"（那只是表象/结果）
- 核心卖点：用得越多系统越强——纠偏变规则、对话沉淀为知识、失败变门禁
- 所有子能力都服务于这个核心：
  - 编码可控 → 为了进化质量
  - 多 Agent 调度协同 → 为了进化效率
  - 端到端研发流水线 → 为了进化完整性
  - 自主差距分析与迭代闭环（OKR-SMART-PDCA）→ 为了进化方向正确
- 竞品错位：Claude Code 卖"更好的程序员助手"，我们卖"会自己进化的 Agent 基础设施"
- 首页文案方向：围绕"自主进化"展开，不围绕"写代码"或"管 Agent"展开

### 2026-05-08
# 2026-05-08 Daily Memory

## 全线差距扫描完成（06:15）

三个项目终局差距扫描结果：
- **SEVO** ✅ 28 FR 全覆盖，差距为零（sa-01 扫描，报告：`reports/sevo-gap-analysis-2026-05-08.md`）
- **Claw Design** ✅ 47 FR 全覆盖，差距为零（audit-01 扫描，报告：`reports/claw-design-gap-analysis-2026-05-08.md`）
- **KIVO** ⚠️ 81 FR 中 65 完全覆盖 / 16 部分覆盖 / 0 未覆盖（昨天 sa-01 扫描，报告：`reports/kivo-gap-scan-2026-05-07.md`）

## KIVO 5 个差距方向 + 修复任务派发（06:25）

用户确认全部并行修复，向量化模型要支持用户自选（本地 Ollama 或云端 API）。

4 路并行派发：
1. **hermes** — 向量化后端可配置（EmbeddingProvider 接口 + init 环境检测 + 云端 API 支持）
2. **cc** — Web 首次体验优化（demo 数据标记 + 导入引导 + 一键清空 demo）
3. **free-code** — 批量萃取审核闭环（低置信候选审核队列）
4. **codex** — 宿主集成通用化（重派，opencode 0 token 失败）

## 调度经验

- 原生 subagent 做大范围代码分析（47+ FR 逐条扫描）不稳定：audit-01、audit-02、sa-02 连续失败（只写模板头/返回无关代码/0 token）。只有 sa-01 成功（SEVO 扫描）。
- sa-01 成功可能因为 SEVO 代码结构更清晰（28 FR vs 47/81 FR）。
- opencode 出现 0 token/0s 实质失败，已记录为不稳定 agent。
- 差距扫描任务需要更显式的步骤指令（列出文件路径、逐条检查、边查边写）才能提高成功率。

## KIVO 实时向量化完成（06:03）

hermes 完成 FR-B04-AC7 实现：
- extraction worker INSERT 后立即调用 `getEmbedding()`
- 5s 超时保护，失败只 log warning 不阻断写入
- 03:15 cron 兜底补建
- 需要 Gateway 重启才能生效（当前 Gateway 加载的是旧版 handler）

## 用户决策记录

- AEO 暂不管，跟 ACO 不是一回事
- 其他项目（SEVO/KIVO/Claw Design）全线并行
- 向量化模型：用户可选本地 Ollama 或云端自配的向量化模型，给提示和指引

## 07:44 KIVO 终局差距修复进度

批次 B（cc）✅ 完成：FR-A04/N06/A06/I03
批次 C（free-code）✅ 完成：FR-A01/A02/A03/E04
批次 A（hermes）🔄 进行中：FR-Z02/Z06/B02/B04
批次 D（dev-01）🔄 进行中：FR-D02/M02/W16/Z04
ACO spec 重写（pm-01）🔄 进行中

ACO vs AEO 定位纠偏已写入 MEMORY.md：
- ACO = 可控调度中枢（前置注入 + 调度铁律）
- AEO = 效果运营平台（观测 + 诊断 + 优化）
- KIVO 系统词典 = 专有名词统一收录与分发（不是迁移，md 文档内容保留）

## 08:05 ACO 核心调度铁律实现进度

hermes ✅ 完成 ACO 批次 1（FR-C01/C02/C03/C04）：
- C01 开发审计分离：enforceDevAuditSeparation + validateAuditAssignment
- C02 失败即时重派：isSubstantiveFailure + handleFailure（3种策略）
- C03 超时熔断：CircuitBreaker class + detectTimeouts + scanStaleTasks
- C04 主会话保护：shouldDelegateToSubagent（40+命令模式）

free-code 🔄 进行中 ACO 批次 2（FR-C05/C06/C07/C08）

KIVO 系统词典：34 条术语已补录，spec FR-H01 AC1 已更新（加 abbreviation/fullName/chineseName 必填字段）

## 08:13 SEVO 主动驱动层缺口确认（用户质疑 + dev-01 验证）

用户质疑：SEVO 的 OKR→SMART→PDCA 链条没有开箱即用自动化生效。

验证结论：
- PostReleaseValidationStage 代码存在，back-edge 机制存在（canComplete=false → 回到 implement）
- 但缺少主动驱动层：没有自动化机制在 deploy 后触发 gap scan
- OKR/KR 达成度检查完全没有自动化（endStateGoal 可设置但无定时检查）
- AC-17.6 明确写了"引擎不做差距判定"，需要调度层派 agent 做，但调度层没有被自动提醒

根因：SEVO 是被动状态机，不是主动目标驱动引擎。有骨架无灵魂。

下一步：ACO P1 修完发版后，走 sevo:create sevo 补 SEVO 主动驱动层（自动 gap scan + 自动 KR 检查 + 自动修复派发）

## 官网页面生成方法溯源（12:15 用户纠偏，永久生效）

### 事实
- `/var/www/html/harness-design/claw-design.html`（32KB/444行）是 ux-01 在 5/6 直接手写的
- 技术栈（历史制作时）：Tailwind CSS CDN + Alpine.js CDN + Lucide Icons + Canvas 粒子背景（注：同日后续用户决策已改为纯 HTML + 自定义 CSS + vanilla JS）
- Claw Design 产品的 landing-page skill（`skill/landing-page/scripts/generate.ts`）只是个 stub，产出质量极低
- claw-design-prototype skill 产出的是 50KB 限制的快速原型，不适合生产级页面

### 正确做法
- 官网页面必须由 **ux-01** 来做（视觉设计专属铁律）
- 技术栈固定：纯 HTML + 自定义 CSS + vanilla JS（同日后续用户纠偏，覆盖早期 Tailwind 方案）
- 新页面必须先读 claw-design.html 作为风格参考模板
- 导航栏已定稿，直接复制 claw-design.html 的 header 结构

### 教训
- 质量差距明显时，第一反应应该是溯源考古（git log + memory search），不是凭猜测重试
- 关键方法论（谁做的、用什么做的、怎么做的）必须写入 memory，不能任由遗忘
- 编码 Agent（cc/free-code/hermes）做视觉设计 = 违反铁律，产出必然不如 ux-01

## 硬伤纠偏：不观察就判断（12:17 用户纠偏，永久生效）

### 问题
- 遇到任务时直接跳到「怎么做」，跳过「先看看现状是什么」
- 明明文件就在那里（kivo.html/sevo.html/aco.html 全部已部署），却没有先 ls 确认就开始重新生成
- PDCA 的 Check 环节被习惯性跳过

### 自检清单（每次接到任务时强制过）
1. 现状是什么？（ls/cat/grep 先看）
2. 之前怎么做的？（git log 溯源）
3. 已有资产在哪？（文件系统 + git history）
4. 差距是什么？（现状 vs 目标）
5. 最小动作是什么？（只补差距，不重做已有的）

### 官网现状（事实）
- 部署目录：`/var/www/html/harness-design/`
- 已有页面：index.html(ux-01刚重做) / kivo.html / sevo.html / aco.html / aeo.html / claw-design.html
- 项目备份：`workspace/projects/homepage/design/`（同步副本）
- 技术栈：Tailwind CDN + Alpine.js + Lucide + Canvas 粒子
- 制作者：ux-01