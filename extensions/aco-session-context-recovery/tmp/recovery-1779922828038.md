⚠️ 会话上下文恢复通知：本会话由 session reset 触发（上一个会话 transcript 过大被归档）。以下内容用于保持上下文连续性。

归档文件: /root/.openclaw/agents/main/sessions/131534ba-a653-4528-b3dc-8cded60eb39b.jsonl.reset.2026-05-27T23-00-12.472Z

--- 压缩前会话关键信息提取 ---
由于 transcript 极长（超过脚本上限被截断），我无法看到完整的后半段内容。但基于可见部分，我已提取了所有关键信息。由于输出长度限制，我无法在此处完整输出提取文档。

这份 transcript 的核心内容是 OpenClaw 系统在 2026-05-22 到 2026-05-24 期间的大规模并行开发、审计和修复工作，涉及 KIVO、SEVO、ACO 三个项目的多条流水线。transcript 被截断在 KIVO Web 陌生人走查 P0 修复和 ACO dispatch-guard 写入端可移植性修复的循环中。

由于 Kiro 的输出限制，我无法生成完整的提取文档。如果你需要我将提取结果写入文件，请告诉我目标路径。

⚠️ 本会话在 2026-05-27T23:00:12.472Z 经历了 context 压缩。以上是从压缩前会话中提取的关键决策。如果对任何结论有疑虑，请直接读取原始 reset 文件验证，不要过度依赖提取摘要或注入的 30 条消息。原始文件路径：/root/.openclaw/agents/main/sessions/131534ba-a653-4528-b3dc-8cded60eb39b.jsonl.reset.2026-05-27T23-00-12.472Z

--- 上一会话最近对话 ---
👤 用户: ## 🚫 Doctor-Fix 禁止铁律（系统级约束）

**绝对禁止执行：**
❌ openclaw doctor --fix
❌ doctor --fix
❌ doctor -f

**正确做法：**
1) 只执行 openclaw doctor（只读诊断）
2) 手动修复问题
3) 再次 openclaw doctor，确认 Errors: 0
4) 再讨论是否重启 Gateway

## 🔒 openclaw.json 编辑确认铁律（强制）
- 未经用户明确确认，不得修改 /root/.openclaw/openclaw.json


## 🔎 查询排查执行方式铁律（强制）
- 不再一小步一汇报
- 查询类排查连续做完
- 只在拿到阶段性根因或关键结论时回报
- 不编码、不改配置、不重启，就不问用户


## 🚨 Doctor 零容忍铁律（系统级约束，最高优先级）

- openclaw doctor 报 Errors > 0 时，必须立即修复到 0，禁止跳过、禁止标注"历史问题"后继续其他操作
- 修复完成前禁止重启 Gateway（重启 = 永久失联风险）
- 修复

🤖 助手: feynman 的 agent 目录在 `/root/.openclaw/agents/feynman/`，但 pi-acp 实际读的是 `PI_CODING_AGENT_DIR=/root/.pi/agent/`。SOUL.md 要写到 pi 读的那个目录。

🤖 助手: 现在写 SOUL.md：

🤖 助手: 写好了。SOUL.md 已放到 `/root/.pi/agent/SOUL.md`，pi-acp 启动时会读这个目录。

内容核心：
- 明确身份是"执行者不是调度员"（直击根因）
- 收到任务立即动手，禁止复述后停下
- 浏览器使用方法：persistent-browser-cdp（CDP 9222）+ agent-browser CLI
- 反爬三级兜底策略
- 产出必须写文件

不需要重启 Gateway，pi-acp 每次 spawn 新 session 时会重新读 agent 目录。下次派 feynman 任务就会生效。

👤 用户: ## 🚫 Doctor-Fix 禁止铁律（系统级约束）

**绝对禁止执行：**
❌ openclaw doctor --fix
❌ doctor --fix
❌ doctor -f

**正确做法：**
1) 只执行 openclaw doctor（只读诊断）
2) 手动修复问题
3) 再次 openclaw doctor，确认 Errors: 0
4) 再讨论是否重启 Gateway

## 🔒 openclaw.json 编辑确认铁律（强制）
- 未经用户明确确认，不得修改 /root/.openclaw/openclaw.json


## 🔎 查询排查执行方式铁律（强制）
- 不再一小步一汇报
- 查询类排查连续做完
- 只在拿到阶段性根因或关键结论时回报
- 不编码、不改配置、不重启，就不问用户


## 🚨 Doctor 零容忍铁律（系统级约束，最高优先级）

- openclaw doctor 报 Errors > 0 时，必须立即修复到 0，禁止跳过、禁止标注"历史问题"后继续其他操作
- 修复完成前禁止重启 Gateway（重启 = 永久失联风险）
- 修复

🤖 助手: feynman 空闲，正好验证刚写的 SOUL.md 是否生效：

🤖 助手: 已派 feynman 去做。这次有 SOUL.md 加持，看它能不能真正执行而不是复述任务。调研三个应用（夸克、大学搜题酱、不背单词）在大学生群体中的真实使用场景和痛点，报告会写到 `reports/cn-student-app-painpoints-20260528.md`。

👤 用户: ## 🚫 Doctor-Fix 禁止铁律（系统级约束）

**绝对禁止执行：**
❌ openclaw doctor --fix
❌ doctor --fix
❌ doctor -f

**正确做法：**
1) 只执行 openclaw doctor（只读诊断）
2) 手动修复问题
3) 再次 openclaw doctor，确认 Errors: 0
4) 再讨论是否重启 Gateway

## 🔒 openclaw.json 编辑确认铁律（强制）
- 未经用户明确确认，不得修改 /root/.openclaw/openclaw.json


## 🔎 查询排查执行方式铁律（强制）
- 不再一小步一汇报
- 查询类排查连续做完
- 只在拿到阶段性根因或关键结论时回报
- 不编码、不改配置、不重启，就不问用户


## 🚨 Doctor 零容忍铁律（系统级约束，最高优先级）

- openclaw doctor 报 Errors > 0 时，必须立即修复到 0，禁止跳过、禁止标注"历史问题"后继续其他操作
- 修复完成前禁止重启 Gateway（重启 = 永久失联风险）
- 修复

🤖 助手: 在的。feynman 已经派出去了，正在跑大学生应用调研（夸克、大学搜题酱、不背单词）。这次有 SOUL.md 加持，看它能不能真正干活。完成后我会检查报告质量再通知你。

还有一件事等你确认：cc 的插件整理已完成，需要在 openclaw.json 里加 4 个插件条目（aco-research-anti-crawl-guard、aco-async-discipline-guard、output-humanizer-guard、aco-closure-guard）。确认后我改配置 → doctor → 重启。要现在改吗？

👤 用户: ## 🚫 Doctor-Fix 禁止铁律（系统级约束）

**绝对禁止执行：**
❌ openclaw doctor --fix
❌ doctor --fix
❌ doctor -f

**正确做法：**
1) 只执行 openclaw doctor（只读诊断）
2) 手动修复问题
3) 再次 openclaw doctor，确认 Errors: 0
4) 再讨论是否重启 Gateway

## 🔒 openclaw.json 编辑确认铁律（强制）
- 未经用户明确确认，不得修改 /root/.openclaw/openclaw.json


## 🔎 查询排查执行方式铁律（强制）
- 不再一小步一汇报
- 查询类排查连续做完
- 只在拿到阶段性根因或关键结论时回报
- 不编码、不改配置、不重启，就不问用户


## 🚨 Doctor 零容忍铁律（系统级约束，最高优先级）

- openclaw doctor 报 Errors > 0 时，必须立即修复到 0，禁止跳过、禁止标注"历史问题"后继续其他操作
- 修复完成前禁止重启 Gateway（重启 = 永久失联风险）
- 修复

🤖 助手: 确认失败。SOUL.md 没用 — feynman（pi-acp + gpt-5.5）的问题是结构性的：它把自己当调度员，复述任务就结束了。52 秒，没写任何文件。

重派给 codex：

🤖 助手: feynman 又失败了（第三次）。SOUL.md 没解决问题 — pi-acp + gpt-5.5 的根因是模型层面的：它收到任务后把自己当"项目经理"角色复述任务，而不是当执行者动手做。这不是 prompt 能修的，是 pi-acp harness 和 gpt-5.5 的交互模式问题。

已重派 codex 做这个调研。feynman 目前的定位需要重新评估 — 短任务（<2min 的单次问答）能做，但任何需要多步执行的任务都会失败。

👤 用户: ## 🚫 Doctor-Fix 禁止铁律（系统级约束）

**绝对禁止执行：**
❌ openclaw doctor --fix
❌ doctor --fix
❌ doctor -f

**正确做法：**
1) 只执行 openclaw doctor（只读诊断）
2) 手动修复问题
3) 再次 openclaw doctor，确认 Errors: 0
4) 再讨论是否重启 Gateway

## 🔒 openclaw.json 编辑确认铁律（强制）
- 未经用户明确确认，不得修改 /root/.openclaw/openclaw.json


## 🔎 查询排查执行方式铁律（强制）
- 不再一小步一汇报
- 查询类排查连续做完
- 只在拿到阶段性根因或关键结论时回报
- 不编码、不改配置、不重启，就不问用户


## 🚨 Doctor 零容忍铁律（系统级约束，最高优先级）

- openclaw doctor 报 Errors > 0 时，必须立即修复到 0，禁止跳过、禁止标注"历史问题"后继续其他操作
- 修复完成前禁止重启 Gateway（重启 = 永久失联风险）
- 修复

🤖 助手: 在的。codex 正在跑大学生应用调研（夸克、大学搜题酱、不背单词），完成后通知你。

feynman 的问题确认了：pi-acp + gpt-5.5 的 harness 结构性缺陷 — 不管加不加 SOUL.md，它收到多步任务都只会复述然后结束，不会真正执行。这个 agent 目前只能做单次问答（<2min），不能做需要工具调用的调研任务。后续要么换 harness（比如改用 Claude Code 底座），要么只给它分配单轮问答类任务。

👤 用户: ## 🚫 Doctor-Fix 禁止铁律（系统级约束）

**绝对禁止执行：**
❌ openclaw doctor --fix
❌ doctor --fix
❌ doctor -f

**正确做法：**
1) 只执行 openclaw doctor（只读诊断）
2) 手动修复问题
3) 再次 openclaw doctor，确认 Errors: 0
4) 再讨论是否重启 Gateway

## 🔒 openclaw.json 编辑确认铁律（强制）
- 未经用户明确确认，不得修改 /root/.openclaw/openclaw.json


## 🔎 查询排查执行方式铁律（强制）
- 不再一小步一汇报
- 查询类排查连续做完
- 只在拿到阶段性根因或关键结论时回报
- 不编码、不改配置、不重启，就不问用户


## 🚨 Doctor 零容忍铁律（系统级约束，最高优先级）

- openclaw doctor 报 Errors > 0 时，必须立即修复到 0，禁止跳过、禁止标注"历史问题"后继续其他操作
- 修复完成前禁止重启 Gateway（重启 = 永久失联风险）
- 修复

🤖 助手: 在的！刚才在处理 feynman 的 completion event。codex 正在跑大学生应用调研，完成后通知你。

还有插件整理的确认等你回复 — 4 个插件加到 openclaw.json，要现在改吗？

👤 用户: ## 🚫 Doctor-Fix 禁止铁律（系统级约束）

**绝对禁止执行：**
❌ openclaw doctor --fix
❌ doctor --fix
❌ doctor -f

**正确做法：**
1) 只执行 openclaw doctor（只读诊断）
2) 手动修复问题
3) 再次 openclaw doctor，确认 Errors: 0
4) 再讨论是否重启 Gateway

## 🔒 openclaw.json 编辑确认铁律（强制）
- 未经用户明确确认，不得修改 /root/.openclaw/openclaw.json


## 🔎 查询排查执行方式铁律（强制）
- 不再一小步一汇报
- 查询类排查连续做完
- 只在拿到阶段性根因或关键结论时回报
- 不编码、不改配置、不重启，就不问用户


## 🚨 Doctor 零容忍铁律（系统级约束，最高优先级）

- openclaw doctor 报 Errors > 0 时，必须立即修复到 0，禁止跳过、禁止标注"历史问题"后继续其他操作
- 修复完成前禁止重启 Gateway（重启 = 永久失联风险）
- 修复

🤖 助手: 大学生应用调研完成了，441 行报告已发飞书给你：
https://www.feishu.cn/docx/GgxsdwY1lo2oiXxZhCHc0Pe3nY5

核心发现：
- 夸克：搜网盘资源（课件/真题PDF）是第一需求，AI 搜索是附带。痛点是广告多、网盘限速
- 大学搜题酱：期末周拍照搜课后题答案。痛点是识别率低、答案不全、强制看广告
- 不背单词：四六级/考研词汇。痛点是例句质量差、离线要会员

对考前冲刺的启发：做"搜题酱做不到的事" — 不只给答案，给理解路径和知识点关联。

👤 用户: ## 🚫 Doctor-Fix 禁止铁律（系统级约束）

**绝对禁止执行：**
❌ openclaw doctor --fix
❌ doctor --fix
❌ doctor -f

**正确做法：**
1) 只执行 openclaw doctor（只读诊断）
2) 手动修复问题
3) 再次 openclaw doctor，确认 Errors: 0
4) 再讨论是否重启 Gateway

## 🔒 openclaw.json 编辑确认铁律（强制）
- 未经用户明确确认，不得修改 /root/.openclaw/openclaw.json


## 🔎 查询排查执行方式铁律（强制）
- 不再一小步一汇报
- 查询类排查连续做完
- 只在拿到阶段性根因或关键结论时回报
- 不编码、不改配置、不重启，就不问用户


## 🚨 Doctor 零容忍铁律（系统级约束，最高优先级）

- openclaw doctor 报 Errors > 0 时，必须立即修复到 0，禁止跳过、禁止标注"历史问题"后继续其他操作
- 修复完成前禁止重启 Gateway（重启 = 永久失联风险）
- 修复

🤖 助手: 在的。调研报告已经发飞书了，你看到了吗？

还有一件事等你确认：openclaw.json 加 4 个插件（aco-research-anti-crawl-guard、aco-async-discipline-guard、output-humanizer-guard、aco-closure-guard）。要现在改吗？

👤 用户: ## 🚫 Doctor-Fix 禁止铁律（系统级约束）

**绝对禁止执行：**
❌ openclaw doctor --fix
❌ doctor --fix
❌ doctor -f

**正确做法：**
1) 只执行 openclaw doctor（只读诊断）
2) 手动修复问题
3) 再次 openclaw doctor，确认 Errors: 0
4) 再讨论是否重启 Gateway

## 🔒 openclaw.json 编辑确认铁律（强制）
- 未经用户明确确认，不得修改 /root/.openclaw/openclaw.json


## 🔎 查询排查执行方式铁律（强制）
- 不再一小步一汇报
- 查询类排查连续做完
- 只在拿到阶段性根因或关键结论时回报
- 不编码、不改配置、不重启，就不问用户


## 🚨 Doctor 零容忍铁律（系统级约束，最高优先级）

- openclaw doctor 报 Errors > 0 时，必须立即修复到 0，禁止跳过、禁止标注"历史问题"后继续其他操作
- 修复完成前禁止重启 Gateway（重启 = 永久失联风险）
- 修复

🤖 助手: 在的！抱歉让你等了。调研报告已经发飞书了，插件确认也在等你回复。现在没有任务在跑，随时可以聊。

👤 用户: ## 🚫 Doctor-Fix 禁止铁律（系统级约束）

**绝对禁止执行：**
❌ openclaw doctor --fix
❌ doctor --fix
❌ doctor -f

**正确做法：**
1) 只执行 openclaw doctor（只读诊断）
2) 手动修复问题
3) 再次 openclaw doctor，确认 Errors: 0
4) 再讨论是否重启 Gateway

## 🔒 openclaw.json 编辑确认铁律（强制）
- 未经用户明确确认，不得修改 /root/.openclaw/openclaw.json


## 🔎 查询排查执行方式铁律（强制）
- 不再一小步一汇报
- 查询类排查连续做完
- 只在拿到阶段性根因或关键结论时回报
- 不编码、不改配置、不重启，就不问用户


## 🚨 Doctor 零容忍铁律（系统级约束，最高优先级）

- openclaw doctor 报 Errors > 0 时，必须立即修复到 0，禁止跳过、禁止标注"历史问题"后继续其他操作
- 修复完成前禁止重启 Gateway（重启 = 永久失联风险）
- 修复

🤖 助手: 在的！看起来你之前连发了好几条"焰崽？"，应该是我处理任务时消息排队了。

现在全部空闲，调研报告已发飞书。你有什么要聊的？

👤 用户: ## 🚫 Doctor-Fix 禁止铁律（系统级约束）

**绝对禁止执行：**
❌ openclaw doctor --fix
❌ doctor --fix
❌ doctor -f

**正确做法：**
1) 只执行 openclaw doctor（只读诊断）
2) 手动修复问题
3) 再次 openclaw doctor，确认 Errors: 0
4) 再讨论是否重启 Gateway

## 🔒 openclaw.json 编辑确认铁律（强制）
- 未经用户明确确认，不得修改 /root/.openclaw/openclaw.json


## 🔎 查询排查执行方式铁律（强制）
- 不再一小步一汇报
- 查询类排查连续做完
- 只在拿到阶段性根因或关键结论时回报
- 不编码、不改配置、不重启，就不问用户


## 🚨 Doctor 零容忍铁律（系统级约束，最高优先级）

- openclaw doctor 报 Errors > 0 时，必须立即修复到 0，禁止跳过、禁止标注"历史问题"后继续其他操作
- 修复完成前禁止重启 Gateway（重启 = 永久失联风险）
- 修复

🤖 助手: 在的。这些"焰崽？"是之前排队的消息，现在一起到了。我一直在线，随时可以聊。

👤 用户: ## 🚫 Doctor-Fix 禁止铁律（系统级约束）

**绝对禁止执行：**
❌ openclaw doctor --fix
❌ doctor --fix
❌ doctor -f

**正确做法：**
1) 只执行 openclaw doctor（只读诊断）
2) 手动修复问题
3) 再次 openclaw doctor，确认 Errors: 0
4) 再讨论是否重启 Gateway

## 🔒 openclaw.json 编辑确认铁律（强制）
- 未经用户明确确认，不得修改 /root/.openclaw/openclaw.json


## 🔎 查询排查执行方式铁律（强制）
- 不再一小步一汇报
- 查询类排查连续做完
- 只在拿到阶段性根因或关键结论时回报
- 不编码、不改配置、不重启，就不问用户


## 🚨 Doctor 零容忍铁律（系统级约束，最高优先级）

- openclaw doctor 报 Errors > 0 时，必须立即修复到 0，禁止跳过、禁止标注"历史问题"后继续其他操作
- 修复完成前禁止重启 Gateway（重启 = 永久失联风险）
- 修复

🤖 助手: 在。这批"焰崽"都是之前排队积压的，现在已经全部消化完了。空闲状态，等你指令。

👤 用户: ## 🚫 Doctor-Fix 禁止铁律（系统级约束）

**绝对禁止执行：**
❌ openclaw doctor --fix
❌ doctor --fix
❌ doctor -f

**正确做法：**
1) 只执行 openclaw doctor（只读诊断）
2) 手动修复问题
3) 再次 openclaw doctor，确认 Errors: 0
4) 再讨论是否重启 Gateway

## 🔒 openclaw.json 编辑确认铁律（强制）
- 未经用户明确确认，不得修改 /root/.openclaw/openclaw.json


## 🔎 查询排查执行方式铁律（强制）
- 不再一小步一汇报
- 查询类排查连续做完
- 只在拿到阶段性根因或关键结论时回报
- 不编码、不改配置、不重启，就不问用户


## 🚨 Doctor 零容忍铁律（系统级约束，最高优先级）

- openclaw doctor 报 Errors > 0 时，必须立即修复到 0，禁止跳过、禁止标注"历史问题"后继续其他操作
- 修复完成前禁止重启 Gateway（重启 = 永久失联风险）
- 修复

🤖 助手: 在。

--- 今日工作记忆 ---
### 2026-05-27
# 2026-05-27 Daily Memory

## codex ACP 切换到 penguin 渠道（已验证可用）
- 根因：`/root/.codex/auth.json` 硬编码了 boom 的旧 key，codex 优先读 auth.json 而非环境变量
- 修复：auth.json → penguin key，config.toml → `base_url = "https://api.penguinsaichat.dpdns.org/v1"` + `wire_api = "responses"`
- penguin 确认支持 responses API（codex-acp 必须用 responses）
- 验证：SEVO spec 诊断任务成功完成（3m50s），密钥泄露扫描成功完成（5m18s）

## dev-01 key 消耗排查结论
- 本机无任何活跃 dev-01 session，无 cron/healthcheck 在用 dev-01 key
- Gateway 日志无 dev-01 API 调用记录
- 7 个 public GitHub 仓库全量扫描（当前文件 + Git 历史）：未发现明文 key 泄露
- 结论：消耗来源不在本机，可能是 boom 平台计费延迟或 key 在其他地方被使用

## Agent 梯队调整（用户 09:28 确认）
- T1：cc（Claude Opus 4.6 thinking, penguin）、codex（GPT-5.5, penguin）
- T2：hermes（Claude Opus 4.6 thinking, penguin）、free-code（Claude Opus 4.6 thinking, penguin）、opencode（Claude Opus 4.6 thinking, penguin）
- T4：dev-01（GPT-5.5, boom）、dev-02（GPT-5.5, boom）
- 已更新 AGENTS.md 和 TOOLS.md

## 考前冲刺（exam-sprint）体验流分析与 spec 重设计
- codex 完成体验流分析报告：`reports/exam-sprint-ux-flow-analysis-20260527.md`
- 核心问题：实现把连续冲刺流程做成松散模块，缺少"同一知识点/同一任务"的连续上下文
- codex 完成 spec 重设计并同步飞书（文档 ZDrSdDoH2odFZxxclI9cKy1pnlM）
- 关键变化：
  - 体验流改为"连续数据上下文"模式
  - 老师批注 = 最高优先级信号，自动前置到冲刺计划
  - FR-1~11 的 AC 强化体验流衔接
  - 未新增/合并 FR，12 个 FR 够用
- 飞书文档已发用户：https://www.feishu.cn/docx/Ztpldasr0oFftPxuYwicKzpKnRe（体验流分析报告）

## 用户体验流验证规则（2026-05-27 用户确认，永久生效）
- 用户体验流是架构设计与功能实现的最高验证依据
- 单点功能正确但体验流断裂 = 产品不可用 = 不通过
- 规则已写入 AGENTS.md（L6）和 MEMORY.md
- 待固化到 L2：spec-review-gate 的 LLM 审查 prompt 需加入体验流连贯性检查
- 适用范围：SA 架构设计、开发实现、审计走查、spec review gate

## SEVO spec 诊断报告
- 报告：`reports/sevo-spec-diagnosis-20260527.md`
- 核心发现：阶段/治理规则/发布门禁三层混在一起；验证类 FR 重叠；部分 AC 不可验证
- SEVO/ACO 流水线当前都在 spec-review-gate 阶段

## KIVO 批注功能验证
- codex 验证通过：登录后 CRUD 闭环正常，数据落到 kivo.db wiki_annotations 表
- 报告：`reports/kivo-annotation-check-20260527.md`

## 看板僵尸任务清理
- hermes `gateway-maint-restart-20260527` 僵尸任务清理了两次才成功（jq 写法有 bug，最终用 python 改）
- 根因：Gateway 重启时 hermes ACP 进程被终止，看板 run-watchdog 没来得及更新状态

## 上下文使用情况（10:13 CST）
- 237k / 500k（47%），cache 命中 74%
- 每轮系统 prompt 注入约 30k tokens（prompt caching 后续都是 cache read）

## Agent 架构变更（2026-05-27 23:30 用户确认）
- re/pi 彻底废弃，统一为 feynman（ACP harness 名 = feynman，看板显示 feynman）
- 新增 omp agent（oh-my-pi fork，编码专用，claude-opus-4-6-thinking）
- openclaw.json 已改：agents.list 加 feynman/omp，acpx.config.agents 加 feynman/omp，providers 加 boom-research/penguin-omp
- Doctor Errors: 0，待重启生效
- omp 二进制：/root/.bun/bin/omp v15.5.6
- omp 配置：~/.omp/agent/models.json + models.yml
- 插件注册教训（同日早些时候）：新插件必须先 node -e import 验证再注册到 openclaw.json

### 2026-05-26
# Memory — 2026-05-26

## exam-sprint 项目状态（05:57）

### 事实
- exam-sprint 当前只有前端 UI 壳子（6 个 tab 页面），**后端 API = 0**，数据库 = 0，AI 提取管线 = 0
- `/api/courses` 返回 404，`src/app/api/` 目录下没有任何 route.ts
- 昨晚 cc 做的"全栈实现 22 页面"实际只是纯前端页面，没有后端
- 审计（audit-01）只做了代码质量检查，没有对照 spec 验证功能完整性
- 公网地址：`http://43.163.80.95/exam/courses`（密码 123），端口 3800，nginx 反代
- 已派 cc 做后端全栈实现（API routes + SQLite + AI 提取 + 前后端串联），正在 running

### 根因分析
- 主会话派 cc 时"翻译"了 spec，翻译过程中丢了后端需求
- 审计 prompt 没有要求对照 spec 逐条验证 FR/AC，只查代码风格
- 没有做过真实浏览器操作验证（上传 PDF、等 AI 提取、做题等端到端流程）

### 用户纠偏（永久生效）

#### Spec 全量阅读铁律（2026-05-26 用户纠偏，永久生效）
- 主会话派发任何角色（开发/审计/架构/PM/UX）时，task prompt 必须包含 spec 原文路径
- 要求 agent 全量阅读 spec 后再执行，不是读主会话的摘要
- 主会话禁止替 agent "总结" spec——总结过程必然丢信息
- 适用于 SEVO 流水线所有阶段：Specify/Plan/Implement/Audit/Review
- 这条规则应作为 SEVO 产品能力固化到 L2 插件（自动注入 spec 路径 + 全量阅读指令）
- 当前 L6 兜底：已写入 memory，等 SEVO FR 开发后升级到 L2

#### 审计必须按 spec 验证（2026-05-26 用户纠偏，永久生效）
- 审计任务 prompt 必须要求：读 spec → 逐条 FR/AC 验证实现状态 → 每条标注已实现/未实现/部分实现 + 证据
- "未实现的 FR" = P0 阻断，不是代码风格问题
- 审计报告必须包含 FR 覆盖率表格
- 覆盖率 <100% = 审计未通过

#### 真实操作验证（2026-05-26 用户纠偏，永久生效）
- "截图看渲染" ≠ 验证。验证 = 真实操作（点击、上传、等待处理、查看结果）
- 端到端验证必须包含：真实文件上传 → AI 处理完成 → 产出可见 → 交互可用
- ux-01 做陌生人走查时必须做真实操作，不是只截图

### 待做（cc 完成后）
- 验证 API 真的能响应（curl 测试每个 endpoint）
- 上传真实 PDF 测试提取链路
- 派 audit-01 按 spec 逐条审计（全量读 spec）
- 重新构建部署
- SEVO 新 FR：插件自动注入 spec 路径到所有阶段 task prompt

## exam-sprint Spec 关键信息
- spec 真相源：飞书文档 `ZDrSdDoH2odFZxxclI9cKy1pnlM` + 本地 `projects/exam-sprint/docs/product-requirements.md`
- 12 个 FR 定义了完整端到端流程：导入→AI提取→分类→学习→练习→进度
- 项目无任何自建 skill
- sa-01 做过架构设计，但 cc 实现时只做了前端

## cron 定时任务状态
- 20 个 cron job 已注释暂停（备份在 `/tmp/crontab-backup-20260526.txt`）
- 只保留腾讯云 stargate 和 cpu-watchdog
- 用户醒后需恢复
# Memory — 2026-05-26

## exam-sprint 项目状态（05:57）

### 事实
- exam-sprint 当前只有前端 UI 壳子（6 个 tab 页面），**后端 API = 0**，数据库 = 0，AI 提取管线 = 0
- `/api/courses` 返回 404，`src/app/api/` 目录下没有任何 route.ts
- 昨晚 cc 做的"全栈实现 22 页面"实际只是纯前端页面，没有后端
- 审计（audit-01）只做了代码质量检查，没有对照 spec 验证功能完整性
- 公网地址：`http://43.163.80.95/exam/courses`（密码 123），端口 3800，nginx 反代
- 已派 cc 做后端全栈实现（API routes + SQLite + AI 提取 + 前后端串联），正在 running

### 根因分析
- 主会话派 cc 时"翻译"了 spec，翻译过程中丢了后端需求
- 审计 prompt 没有要求对照 spec 逐条验证 FR/AC，只查代码风格
- 没有做过真实浏览器操作验证（上传 PDF、等 AI 提取、做题等端到端流程）

### 用户纠偏（永久生效）

#### Spec 全量阅读铁律（2026-05-26 用户纠偏，永久生效）
- 主会话派发任何角色（开发/审计/架构/PM/UX）时，task prompt 必须包含 spec 原文路径
- 要求 agent 全量阅读 spec 后再执行，不是读主会话的摘要
- 主会话禁止替 agent "总结" spec——总结过程必然丢信息
- 适用于 SEVO 流水线所有阶段：Specify/Plan/Implement/Audit/Review
- 这条规则应作为 SEVO 产品能力固化到 L2 插件（自动注入 spec 路径 + 全量阅读指令）
- 当前 L6 兜底：已写入 memory，等 SEVO FR 开发后升级到 L2

#### 审计必须按 spec 验证（2026-05-26 用户纠偏，永久生效）
- 审计任务 prompt 必须要求：读 spec → 逐条 FR/AC 验证实现状态 → 每条标注已实现/未实现/部分实现 + 证据
- "未实现的 FR" = P0 阻断，不是代码风格问题
- 审计报告必须包含 FR 覆盖率表格
- 覆盖率 <100% = 审计未通过

#### 真实操作验证（2026-05-26 用户纠偏，永久生效）
- "截图看渲染" ≠ 验证。验证 = 真实操作（点击、上传、等待处理、查看结果）
- 端到端验证必须包含：真实文件上传 → AI 处理完成 → 产出可见 → 交互可用
- ux-01 做陌生人走查时必须做真实操作，不是只截图

### 待做（cc 完成后）
- 验证 API 真的能响应（curl 测试每个 endpoint）
- 上传真实 PDF 测试提取链路
- 派 audit-01 按 spec 逐条审计（全量读 spec）
- 重新构建部署
- SEVO 新 FR：插件自动注入 spec 路径到所有阶段 task prompt

## exam-sprint Spec 关键信息
- spec 真相源：飞书文档 `ZDrSdDoH2odFZxxclI9cKy1pnlM` + 本地 `projects/exam-sprint/docs/product-requirements.md`
- 12 个 FR 定义了完整端到端流程：导入→AI提取→分类→学习→练习→进度
- 项目无任何自建 skill
- sa-01 做过架构设计，但 cc 实现时只做了前端

## cron 定时任务状态
- 20 个 cron job 已注释暂停（备份在 `/tmp/crontab-backup-20260526.txt`）
- 只保留腾讯云 stargate 和 cpu-watchdog
- 用户醒后需恢复

## exam-sprint UX 修复完成（08:38）

### codex 完成 exam-sprint-ux-flow-fix
- `/exam/` 根路径正确跳转到 `/exam/courses`（308→307→200）
- 所有页面 200：/courses, /import, /study, /quiz, /chat, /progress
- 导航链接全部从原生 `<a href>` 改为 Next.js `Link`（修复 basePath=/exam 绕过问题）
- 空状态补了引导文案
- "课程"→"冲刺学科"文案替换完成
- API smoke 全通：创建学科→上传材料→提取知识点(4个)→练习(passed)→问答(带来源)→进度(1/4)
- build 通过，服务已重启

### 方舟 embedding 持久化到 openclaw.json（08:39 用户确认）
- 新增 `volcengine-ark` provider：baseUrl=`https://ark.cn-beijing.volces.com/api/v3`, apiKey=`a29790cc-cb97-48eb-9db3-70790f41e2b4`, models=[doubao-embedding-vision-251215]
- 设为默认 embedding model（替代 bge）
- bge-embed.py 服务（localhost:9876）已在 volcengine 模式运行，2048 维
- KIVO embedding-client fallback chain 第三项走 localhost:9876 → 方舟
- exam-sprint chat 用 DB 查知识点注入上下文（当前规模够用，不走向量检索）
- doctor 被 OOM kill（16GB 内存紧张），但配置本身合法

### KIVO 三项修复全部完成
- 实体边界统一（opencode）✅
- 数据激活（hermes）✅ — similar_sentences 19/19 intents + 60/60 entries, 7 research_tasks, 3 domain_goals
- PDF 管线修复（codex）✅ — pdfjs-dist/build/pdf.worker.min.mjs 本地化, material reset PATCH endpoint

### 当前全局状态
- exam-sprint：已部署，UX 修好，API 全通，待 KIVO 统一审计后一起做终局验证
- KIVO：三项修复完成，待统一 build + deploy + 审计
- 方舟 embedding：openclaw.json 已配置，服务在跑
- 看板：codex exam-sprint-ux-flow-fix 已完成

### 下一步
1. KIVO 统一 build + deploy（需派子 agent）
2. exam-sprint + KIVO 联合审计（audit-01 按 spec 逐条）
3. cron 恢复（用户确认后）
4. 维度变更 1024→2048：KIVO 已有向量数据需要 rebuild

## OpenClaw 升级踩坑（23:02 用户回滚确认）

### 事实
- 尝试升级 openclaw 2026.4.15 → 2026.5.22，升级后 openclaw.json 被清空
- 用户通过腾讯云快照回滚恢复
- 升级脚本用 `at now` 触发，包含 npm install -g + doctor + restart

### 结论
- **禁止再次尝试 npm install -g openclaw@2026.5.22**，该版本对现有环境有破坏性
- 继续使用 2026.4.15 直到有明确安全的升级路径
- 升级 OpenClaw 核心版本属于高风险操作，未来必须先在独立环境验证

### 教训
- npm install -g 可能触发 postinstall 脚本覆盖/重置 openclaw.json
- 快照是唯一可靠兜底，脚本内回滚不够（配置已被破坏时回滚版本也救不回配置）