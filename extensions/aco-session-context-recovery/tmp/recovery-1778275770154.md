⚠️ 会话上下文恢复通知：本会话由 session reset 触发（上一个会话 transcript 过大被归档）。以下是上一个会话最后的对话摘要，请据此保持上下文连续性。

归档文件: 36ad560e-b6cb-4dae-85bc-0015959d185c.jsonl.reset.2026-05-08T21-29-28.070Z

--- 上一会话最近对话 ---
🤖 助手: Domain H 基础设施实现完成，658 测试全绿：

- **注册表** `brand-registry.ts` — 自动扫描 packages/ 目录，支持按名称/标签/关键词检索
- **注入器** `injector.ts` — `injectDesignSystem('apple')` 自动加载 design.md + 激活 TS 约束
- **质量门禁** `package-validator.ts` — 校验 MD 9 段结构 + TS 字段覆盖率
- **动态切换** `active-package.ts` — constraint-checker 自动跟随当前激活包
- **转换器** `scripts/convert-design-md.ts` — 支持单文件和 `--batch` 批量转换
- **dark-sci-fi 迁移** — 已迁入 `packages/dark-sci-fi/` 新目录结构，向后兼容

下一步：用转换器批量生成 72 个品牌包（需要先拿到 Open Design 的 DESIGN.md 源文件）。我派任务去 clone Open De

🤖 助手: 已派 hermes 去 clone Open Design 仓库并批量生成 72 个品牌包。完成后自动进入审计环节。

👤 用户: ## 🚫 Doctor-Fix 禁止铁律（系统级约束）

**绝对禁止执行：**
❌ openclaw doctor --fix
❌ doctor --fix
❌ doctor -f

**正确做法：**
1) 只执行 openclaw doctor（只读诊断）
2) 手动修复问题
3) 再次 openclaw doctor，确认 Errors: 0
4) 再讨论是否重启 Gateway

## 🔒 openclaw.json 编辑确认铁律（强制）
- 未经用户明确确认，不得修改 /root/.openclaw/openclaw.json


## 🔎 查询排查执行方式铁律（强制）
- 不再一小步一汇报
- 查询类排查连续做完
- 只在拿到阶段性根因或关键结论时回报
- 不编码、不改配置、不重启，就不问用户


## 🚨 Doctor 零容忍铁律（系统级约束，最高优先级）

- openclaw doctor 报 Errors > 0 时，必须立即修复到 0，禁止跳过、禁止标注"历史问题"后继续其他操作
- 修复完成前禁止重启 Gateway（重启 = 永久失联风险）
- 修复

🤖 助手: 品牌包批量生成完成。实际比预期多 — Open Design 仓库有 142 个品牌（不止 72），全部转换了。

最终结果：**143 个品牌包**（142 Open Design + 1 dark-sci-fi），664 测试全绿，build 通过。覆盖 Stripe/Vercel/Apple/GitHub/Linear/Figma/Notion/Spotify/Nike/Tesla/Airbnb/Discord/OpenAI/Claude 等。

按 SEVO 流水线，下一步派审计。

🤖 助手: 已派 audit-01 做质量审计。审计通过后进入发布流程。

👤 用户: ## 🚫 Doctor-Fix 禁止铁律（系统级约束）

**绝对禁止执行：**
❌ openclaw doctor --fix
❌ doctor --fix
❌ doctor -f

**正确做法：**
1) 只执行 openclaw doctor（只读诊断）
2) 手动修复问题
3) 再次 openclaw doctor，确认 Errors: 0
4) 再讨论是否重启 Gateway

## 🔒 openclaw.json 编辑确认铁律（强制）
- 未经用户明确确认，不得修改 /root/.openclaw/openclaw.json


## 🔎 查询排查执行方式铁律（强制）
- 不再一小步一汇报
- 查询类排查连续做完
- 只在拿到阶段性根因或关键结论时回报
- 不编码、不改配置、不重启，就不问用户


## 🚨 Doctor 零容忍铁律（系统级约束，最高优先级）

- openclaw doctor 报 Errors > 0 时，必须立即修复到 0，禁止跳过、禁止标注"历史问题"后继续其他操作
- 修复完成前禁止重启 Gateway（重启 = 永久失联风险）
- 修复

🤖 助手: 审计不通过，3 个 P0 + 4 个 P1。已立即派 hermes 修复：

P0：
1. 143 个包全部缺 `reference.html` — 批量生成
2. 所有包 `components` 为空 — 转换器加组件提取，至少 5 个
3. 所有包 `constraints` 为空 — 自动生成颜色/字体/间距约束规则

P1：
4. `create-package` 脚手架脚本未实现
5. 双层一致性校验（MD vs TS）未实现
6. reference.html 校验未实现
7. injector 单品牌约束隔离

修复完成后自动复验。

👤 用户: ## 🚫 Doctor-Fix 禁止铁律（系统级约束）

**绝对禁止执行：**
❌ openclaw doctor --fix
❌ doctor --fix
❌ doctor -f

**正确做法：**
1) 只执行 openclaw doctor（只读诊断）
2) 手动修复问题
3) 再次 openclaw doctor，确认 Errors: 0
4) 再讨论是否重启 Gateway

## 🔒 openclaw.json 编辑确认铁律（强制）
- 未经用户明确确认，不得修改 /root/.openclaw/openclaw.json


## 🔎 查询排查执行方式铁律（强制）
- 不再一小步一汇报
- 查询类排查连续做完
- 只在拿到阶段性根因或关键结论时回报
- 不编码、不改配置、不重启，就不问用户


## 🚨 Doctor 零容忍铁律（系统级约束，最高优先级）

- openclaw doctor 报 Errors > 0 时，必须立即修复到 0，禁止跳过、禁止标注"历史问题"后继续其他操作
- 修复完成前禁止重启 Gateway（重启 = 永久失联风险）
- 修复

🤖 助手: P0/P1 修复完成：143 个包全部有 reference.html，components 不再为空。但 completion 被截断了，可能还有 1 个测试失败（Nike 包字体提取 bug）。让我快速确认 build 状态：

🤖 助手: Nike 字体已修复。让我确认测试状态 — 派 hermes 跑一下 build+test 确认全绿，然后复验：

🤖 助手: P0/P1 修复已完成（143 包全部有 reference.html + components + constraints），已派 hermes 做最终验证 + 发布。等 completion 后通知你。

👤 用户: ## 🚫 Doctor-Fix 禁止铁律（系统级约束）

**绝对禁止执行：**
❌ openclaw doctor --fix
❌ doctor --fix
❌ doctor -f

**正确做法：**
1) 只执行 openclaw doctor（只读诊断）
2) 手动修复问题
3) 再次 openclaw doctor，确认 Errors: 0
4) 再讨论是否重启 Gateway

## 🔒 openclaw.json 编辑确认铁律（强制）
- 未经用户明确确认，不得修改 /root/.openclaw/openclaw.json


## 🔎 查询排查执行方式铁律（强制）
- 不再一小步一汇报
- 查询类排查连续做完
- 只在拿到阶段性根因或关键结论时回报
- 不编码、不改配置、不重启，就不问用户


## 🚨 Doctor 零容忍铁律（系统级约束，最高优先级）

- openclaw doctor 报 Errors > 0 时，必须立即修复到 0，禁止跳过、禁止标注"历史问题"后继续其他操作
- 修复完成前禁止重启 Gateway（重启 = 永久失联风险）
- 修复

🤖 助手: Done. `@self-evolving-harness/claw-design@0.3.1` 已发布。

总结：
- 143 个品牌包内置（Apple/Stripe/Vercel/GitHub/Linear/Figma/Notion/Spotify/Nike/Tesla/Claude/OpenAI 等全部 Open Design 品牌）
- 每个包三文件结构：`design.md`（prompt context）+ `index.ts`（TS 硬约束）+ `reference.html`（视觉参考）
- 基础设施：注册表、按需注入、质量门禁、约束校验、批量转换器、脚手架脚本
- 664 测试全绿，审计通过
- 已推送 npm + GitHub（主仓库 + 独立仓库）

用户体验：说"用 Stripe 风格做一个 landing page"→ 系统自动加载 Stripe 的 design.md 注入 prompt + 激活 TS 约束校验生成结果。

👤 用户: ## 🚫 Doctor-Fix 禁止铁律（系统级约束）

**绝对禁止执行：**
❌ openclaw doctor --fix
❌ doctor --fix
❌ doctor -f

**正确做法：**
1) 只执行 openclaw doctor（只读诊断）
2) 手动修复问题
3) 再次 openclaw doctor，确认 Errors: 0
4) 再讨论是否重启 Gateway

## 🔒 openclaw.json 编辑确认铁律（强制）
- 未经用户明确确认，不得修改 /root/.openclaw/openclaw.json


## 🔎 查询排查执行方式铁律（强制）
- 不再一小步一汇报
- 查询类排查连续做完
- 只在拿到阶段性根因或关键结论时回报
- 不编码、不改配置、不重启，就不问用户


## 🚨 Doctor 零容忍铁律（系统级约束，最高优先级）

- openclaw doctor 报 Errors > 0 时，必须立即修复到 0，禁止跳过、禁止标注"历史问题"后继续其他操作
- 修复完成前禁止重启 Gateway（重启 = 永久失联风险）
- 修复

🤖 助手: 看完两份竞品报告，我认为还有 3 个值得借鉴的点（品牌包已经搞定了）：

**1. Slop 黑名单（视觉反模式清单）**

Open Design 有一份明确的"禁止清单"：暴力紫渐变、通用 emoji 图标、Inter 当 display 字体、手绘 SVG 真人脸、编造数据等。这比我们现在 Domain G 的 constraint-checker 更具体 — 我们的约束是"品牌内合规"，但缺少"通用 AI 味反模式"的硬拦截。

可以做成一个全局 slop-blacklist，不管用什么品牌包都生效。

**2. 实时预览**

这是体验差距最大的地方。Open Design 用 iframe 沙盒实时渲染，用户边看边改。我们现在是生成 HTML → 打包 → 用户自己打开看。中间有体验断裂。

不过这个跟 Claw Design 的定位（Skill 包/引擎）有冲突 — 实时预览需要一个 Web 界面。如果要做，可能是一个独立的 playground 页面（类似 agentos.site 上挂一个 /playground），用户粘贴生成的 HTML 即时预览。

**3. 过

👤 用户: ## 🚫 Doctor-Fix 禁止铁律（系统级约束）

**绝对禁止执行：**
❌ openclaw doctor --fix
❌ doctor --fix
❌ doctor -f

**正确做法：**
1) 只执行 openclaw doctor（只读诊断）
2) 手动修复问题
3) 再次 openclaw doctor，确认 Errors: 0
4) 再讨论是否重启 Gateway

## 🔒 openclaw.json 编辑确认铁律（强制）
- 未经用户明确确认，不得修改 /root/.openclaw/openclaw.json


## 🔎 查询排查执行方式铁律（强制）
- 不再一小步一汇报
- 查询类排查连续做完
- 只在拿到阶段性根因或关键结论时回报
- 不编码、不改配置、不重启，就不问用户


## 🚨 Doctor 零容忍铁律（系统级约束，最高优先级）

- openclaw doctor 报 Errors > 0 时，必须立即修复到 0，禁止跳过、禁止标注"历史问题"后继续其他操作
- 修复完成前禁止重启 Gateway（重启 = 永久失联风险）
- 修复

🤖 助手: 已派 pm-01 写 spec，完成后自动进实现。

👤 用户: ## 🚫 Doctor-Fix 禁止铁律（系统级约束）

**绝对禁止执行：**
❌ openclaw doctor --fix
❌ doctor --fix
❌ doctor -f

**正确做法：**
1) 只执行 openclaw doctor（只读诊断）
2) 手动修复问题
3) 再次 openclaw doctor，确认 Errors: 0
4) 再讨论是否重启 Gateway

## 🔒 openclaw.json 编辑确认铁律（强制）
- 未经用户明确确认，不得修改 /root/.openclaw/openclaw.json


## 🔎 查询排查执行方式铁律（强制）
- 不再一小步一汇报
- 查询类排查连续做完
- 只在拿到阶段性根因或关键结论时回报
- 不编码、不改配置、不重启，就不问用户


## 🚨 Doctor 零容忍铁律（系统级约束，最高优先级）

- openclaw doctor 报 Errors > 0 时，必须立即修复到 0，禁止跳过、禁止标注"历史问题"后继续其他操作
- 修复完成前禁止重启 Gateway（重启 = 永久失联风险）
- 修复

🤖 助手: 你说得对。我在套竞品的功能列表，没从 Claw Design 自身定位出发思考。

实时预览 — Claw Design 是 Skill 包/引擎，不拥有 UI。渲染是宿主平台的事。给一个库加 playground 等于在做一个新产品，跟定位冲突。

过程可见性 — 生成过程就是一次 LLM 调用 + 质量门禁。没有多步流水线可报告。宿主平台本身就有 streaming。给 Skill 包加进度推送是过度设计。

这两个都是 Open Design 作为独立桌面应用才需要的东西，搬到 Skill 包形态上没有意义。slop 黑名单已经在跑了。

👤 用户: ## 🚫 Doctor-Fix 禁止铁律（系统级约束）

**绝对禁止执行：**
❌ openclaw doctor --fix
❌ doctor --fix
❌ doctor -f

**正确做法：**
1) 只执行 openclaw doctor（只读诊断）
2) 手动修复问题
3) 再次 openclaw doctor，确认 Errors: 0
4) 再讨论是否重启 Gateway

## 🔒 openclaw.json 编辑确认铁律（强制）
- 未经用户明确确认，不得修改 /root/.openclaw/openclaw.json


## 🔎 查询排查执行方式铁律（强制）
- 不再一小步一汇报
- 查询类排查连续做完
- 只在拿到阶段性根因或关键结论时回报
- 不编码、不改配置、不重启，就不问用户


## 🚨 Doctor 零容忍铁律（系统级约束，最高优先级）

- openclaw doctor 报 Errors > 0 时，必须立即修复到 0，禁止跳过、禁止标注"历史问题"后继续其他操作
- 修复完成前禁止重启 Gateway（重启 = 永久失联风险）
- 修复

🤖 助手: Slop 黑名单 spec 已写入。pm-01 追加了 3 个 FR 到 Domain D：

- **FR-D09**：20 条内置 AI 味反模式规则（暴力紫渐变、Inter 当标题字体、通用 emoji 图标、过度 drop shadow 等），基于 HTML/CSS DOM 自动检测
- **FR-D10**：黑名单可配置（用户可增删规则、调 severity）
- **FR-D11**：检查管线（品牌约束检查之后执行，命中 block 级规则强制重做，最多 3 次）

总 FR 数 60→63。现在派 hermes 实现。

🤖 助手: hermes 已开始实现。完成后自动走审计链。

👤 用户: ## 🚫 Doctor-Fix 禁止铁律（系统级约束）

**绝对禁止执行：**
❌ openclaw doctor --fix
❌ doctor --fix
❌ doctor -f

**正确做法：**
1) 只执行 openclaw doctor（只读诊断）
2) 手动修复问题
3) 再次 openclaw doctor，确认 Errors: 0
4) 再讨论是否重启 Gateway

## 🔒 openclaw.json 编辑确认铁律（强制）
- 未经用户明确确认，不得修改 /root/.openclaw/openclaw.json


## 🔎 查询排查执行方式铁律（强制）
- 不再一小步一汇报
- 查询类排查连续做完
- 只在拿到阶段性根因或关键结论时回报
- 不编码、不改配置、不重启，就不问用户


## 🚨 Doctor 零容忍铁律（系统级约束，最高优先级）

- openclaw doctor 报 Errors > 0 时，必须立即修复到 0，禁止跳过、禁止标注"历史问题"后继续其他操作
- 修复完成前禁止重启 Gateway（重启 = 永久失联风险）
- 修复

🤖 助手: Now dispatch audit-01:

🤖 助手: Slop 黑名单实现完成，725 测试全绿。已派 audit-01 做质量审计，通过后自动 bump 版本发布。

👤 用户: ## 🚫 Doctor-Fix 禁止铁律（系统级约束）

**绝对禁止执行：**
❌ openclaw doctor --fix
❌ doctor --fix
❌ doctor -f

**正确做法：**
1) 只执行 openclaw doctor（只读诊断）
2) 手动修复问题
3) 再次 openclaw doctor，确认 Errors: 0
4) 再讨论是否重启 Gateway

## 🔒 openclaw.json 编辑确认铁律（强制）
- 未经用户明确确认，不得修改 /root/.openclaw/openclaw.json


## 🔎 查询排查执行方式铁律（强制）
- 不再一小步一汇报
- 查询类排查连续做完
- 只在拿到阶段性根因或关键结论时回报
- 不编码、不改配置、不重启，就不问用户


## 🚨 Doctor 零容忍铁律（系统级约束，最高优先级）

- openclaw doctor 报 Errors > 0 时，必须立即修复到 0，禁止跳过、禁止标注"历史问题"后继续其他操作
- 修复完成前禁止重启 Gateway（重启 = 永久失联风险）
- 修复

🤖 助手: 审计通过（有条件），2 个 P1 需要修。派 hermes 修复后直接发版。

🤖 助手: hermes 在修 P1 + 发版，完成后推送。

👤 用户: ## 🚫 Doctor-Fix 禁止铁律（系统级约束）

**绝对禁止执行：**
❌ openclaw doctor --fix
❌ doctor --fix
❌ doctor -f

**正确做法：**
1) 只执行 openclaw doctor（只读诊断）
2) 手动修复问题
3) 再次 openclaw doctor，确认 Errors: 0
4) 再讨论是否重启 Gateway

## 🔒 openclaw.json 编辑确认铁律（强制）
- 未经用户明确确认，不得修改 /root/.openclaw/openclaw.json


## 🔎 查询排查执行方式铁律（强制）
- 不再一小步一汇报
- 查询类排查连续做完
- 只在拿到阶段性根因或关键结论时回报
- 不编码、不改配置、不重启，就不问用户


## 🚨 Doctor 零容忍铁律（系统级约束，最高优先级）

- openclaw doctor 报 Errors > 0 时，必须立即修复到 0，禁止跳过、禁止标注"历史问题"后继续其他操作
- 修复完成前禁止重启 Gateway（重启 = 永久失联风险）
- 修复

🤖 助手: Slop 黑名单全链路闭环：spec → 实现 → 审计 → P1 修复 → 发布 `@self-evolving-harness/claw-design@0.3.2`。

今晚 Claw Design 两个大动作都落地了：
1. **v0.3.1** — Domain H 品牌包生态（143 个品牌包内置）
2. **v0.3.2** — Slop 黑名单（20 条 AI 味反模式硬拦截 + quality-gate 集成）

--- 今日工作记忆 ---
### 2026-05-08
# 2026-05-08 Daily Memory

## 全线差距扫描完成（06:15）

三个项目终局差距扫描结果：
- **SEVO** ✅ 28 FR 全覆盖，差距为零（sa-01 扫描，报告：`reports/sevo-gap-analysis-2026-05-08.md`）
- **Claw Design** ✅ 47 FR 全覆盖，差距为零（audit-01 扫描，报告：`reports/claw-design-gap-analysis-2026-05-08.md`）
- **KIVO** ⚠️ 81 FR 中 65 完全覆盖 / 16 部分覆盖 / 0 未覆盖（昨天 sa-01 扫描，报告：`reports/kivo-gap-scan-2026-05-07.md`）

## KIVO 5 个差距方向 + 修复任务派发（06:25）

用户确认全部并行修复，向量化模型要支持用户自选（本地 Ollama 或云端 API）。

4 路并行派发：
1. **hermes** — 向量化后端可配置（EmbeddingProvider 接口 + init 环境检测 + 云端 API 支持）
2. **cc** — Web 首次体验优化（demo 数据标记 + 导入引导 + 一键清空 demo）
3. **free-code** — 批量萃取审核闭环（低置信候选审核队列）
4. **codex** — 宿主集成通用化（重派，opencode 0 token 失败）

## 调度经验

- 原生 subagent 做大范围代码分析（47+ FR 逐条扫描）不稳定：audit-01、audit-02、sa-02 连续失败（只写模板头/返回无关代码/0 token）。只有 sa-01 成功（SEVO 扫描）。
- sa-01 成功可能因为 SEVO 代码结构更清晰（28 FR vs 47/81 FR）。
- opencode 出现 0 token/0s 实质失败，已记录为不稳定 agent。
- 差距扫描任务需要更显式的步骤指令（列出文件路径、逐条检查、边查边写）才能提高成功率。

## KIVO 实时向量化完成（06:03）

hermes 完成 FR-B04-AC7 实现：
- extraction worker INSERT 后立即调用 `getEmbedding()`
- 5s 超时保护，失败只 log warning 不阻断写入
- 03:15 cron 兜底补建
- 需要 Gateway 重启才能生效（当前 Gateway 加载的是旧版 handler）

## 用户决策记录

- AEO 暂不管，跟 ACO 不是一回事
- 其他项目（SEVO/KIVO/Claw Design）全线并行
- 向量化模型：用户可选本地 Ollama 或云端自配的向量化模型，给提示和指引

## 07:44 KIVO 终局差距修复进度

批次 B（cc）✅ 完成：FR-A04/N06/A06/I03
批次 C（free-code）✅ 完成：FR-A01/A02/A03/E04
批次 A（hermes）🔄 进行中：FR-Z02/Z06/B02/B04
批次 D（dev-01）🔄 进行中：FR-D02/M02/W16/Z04
ACO spec 重写（pm-01）🔄 进行中

ACO vs AEO 定位纠偏已写入 MEMORY.md：
- ACO = 可控调度中枢（前置注入 + 调度铁律）
- AEO = 效果运营平台（观测 + 诊断 + 优化）
- KIVO 系统词典 = 专有名词统一收录与分发（不是迁移，md 文档内容保留）

## 08:05 ACO 核心调度铁律实现进度

hermes ✅ 完成 ACO 批次 1（FR-C01/C02/C03/C04）：
- C01 开发审计分离：enforceDevAuditSeparation + validateAuditAssignment
- C02 失败即时重派：isSubstantiveFailure + handleFailure（3种策略）
- C03 超时熔断：CircuitBreaker class + detectTimeouts + scanStaleTasks
- C04 主会话保护：shouldDelegateToSubagent（40+命令模式）

free-code 🔄 进行中 ACO 批次 2（FR-C05/C06/C07/C08）

KIVO 系统词典：34 条术语已补录，spec FR-H01 AC1 已更新（加 abbreviation/fullName/chineseName 必填字段）

## 08:13 SEVO 主动驱动层缺口确认（用户质疑 + dev-01 验证）

用户质疑：SEVO 的 OKR→SMART→PDCA 链条没有开箱即用自动化生效。

验证结论：
- PostReleaseValidationStage 代码存在，back-edge 机制存在（canComplete=false → 回到 implement）
- 但缺少主动驱动层：没有自动化机制在 deploy 后触发 gap scan
- OKR/KR 达成度检查完全没有自动化（endStateGoal 可设置但无定时检查）
- AC-17.6 明确写了"引擎不做差距判定"，需要调度层派 agent 做，但调度层没有被自动提醒

根因：SEVO 是被动状态机，不是主动目标驱动引擎。有骨架无灵魂。

下一步：ACO P1 修完发版后，走 sevo:create sevo 补 SEVO 主动驱动层（自动 gap scan + 自动 KR 检查 + 自动修复派发）

## 官网页面生成方法溯源（12:15 用户纠偏，永久生效）

### 事实
- `/var/www/html/harness-design/claw-design.html`（32KB/444行）是 ux-01 在 5/6 直接手写的
- 技术栈（历史制作时）：Tailwind CSS CDN + Alpine.js CDN + Lucide Icons + Canvas 粒子背景（注：同日后续用户决策已改为纯 HTML + 自定义 CSS + vanilla JS）
- Claw Design 产品的 landing-page skill（`skill/landing-page/scripts/generate.ts`）只是个 stub，产出质量极低
- claw-design-prototype skill 产出的是 50KB 限制的快速原型，不适合生产级页面

### 正确做法
- 官网页面必须由 **ux-01** 来做（视觉设计专属铁律）
- 技术栈固定：纯 HTML + 自定义 CSS + vanilla JS（同日后续用户纠偏，覆盖早期 Tailwind 方案）
- 新页面必须先读 claw-design.html 作为风格参考模板
- 导航栏已定稿，直接复制 claw-design.html 的 header 结构

### 教训
- 质量差距明显时，第一反应应该是溯源考古（git log + memory search），不是凭猜测重试
- 关键方法论（谁做的、用什么做的、怎么做的）必须写入 memory，不能任由遗忘
- 编码 Agent（cc/free-code/hermes）做视觉设计 = 违反铁律，产出必然不如 ux-01

## 硬伤纠偏：不观察就判断（12:17 用户纠偏，永久生效）

### 问题
- 遇到任务时直接跳到「怎么做」，跳过「先看看现状是什么」
- 明明文件就在那里（kivo.html/sevo.html/aco.html 全部已部署），却没有先 ls 确认就开始重新生成
- PDCA 的 Check 环节被习惯性跳过

### 自检清单（每次接到任务时强制过）
1. 现状是什么？（ls/cat/grep 先看）
2. 之前怎么做的？（git log 溯源）
3. 已有资产在哪？（文件系统 + git history）
4. 差距是什么？（现状 vs 目标）
5. 最小动作是什么？（只补差距，不重做已有的）

### 官网现状（事实）
- 部署目录：`/var/www/html/harness-design/`
- 已有页面：index.html(ux-01刚重做) / kivo.html / sevo.html / aco.html / aeo.html / claw-design.html
- 项目备份：`workspace/projects/homepage/design/`（同步副本）
- 技术栈：Tailwind CDN + Alpine.js + Lucide + Canvas 粒子
- 制作者：ux-01